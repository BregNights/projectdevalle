spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

google:
  maps:
    api-key: dummy-key-not-used-in-tests
    connect-timeout: 3s
    read-timeout: 5s
</content>
